/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import navMainStack from './src/init';

AppRegistry.registerComponent(appName, () => navMainStack);
